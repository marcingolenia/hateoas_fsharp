{
  "private": true,
  "scripts": {
    "postinstall": "dotnet tool restore",
    "start-fable": "dotnet fable watch src",
    "start-vite": "vite",
    "test": "dotnet fable tests -o build/tests --run web-test-runner build/tests/*Test.js",
    "test-fable:watch": "dotnet fable watch tests -o build/tests",
    "test-wtr:watch": "web-test-runner build/tests/*Test.js --watch"
  },
  "devDependencies": {
    "@web/test-runner": "^0.13.18",
    "@web/test-runner-commands": "^0.5.13",
    "autoprefixer": "^10.4.18",
    "daisyui": "^4.7.2",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1",
    "vite": "^5.1.5"
  },
  "version": "1.0.0"
}
